## [1.2.5]

#### 2026-07-09

### 权限控制
- **清空历史按钮权限**：清空历史按钮添加登录权限控制，未登录状态下自动隐藏
- **重复检测按钮权限**：重复检测按钮添加登陆权限控制，未登陆状态下自动隐藏
- **封面管理按钮权限**：封面管理按钮添加登陆权限控制，未登陆状态下自动隐藏
- **测试声卡按钮权限**：测试声卡按钮添加登陆权限控制，未登陆状态下自动隐藏
- **应用选中按钮权限**：应用选中按钮添加登陆权限控制，未登陆状态下自动隐藏
- **上传音源按钮权限**：上传音源按钮添加登陆权限控制，未登陆状态下自动隐藏
- **刷新列表按钮权限**：刷新列表按钮添加登陆权限控制，未登陆状态下自动隐藏
- **线程数量设置按钮权限**：线程数量设置按钮添加登陆权限控制，未登陆状态下自动隐藏
- **清除已完成任务按钮权限**：清除已完成任务按钮添加登陆权限控制，未登陆状态下自动隐藏


### 问题修复
- **音源文件管理列表选中状态**：修复未登录状态下音源文件管理列表无法显示已选中音源的问题。原因是 `loadOnlineSettings` 函数在 `activeSources` 为空数组时不会设置 `window.activeSources`，导致渲染时无法正确显示选中状态
- **退出登录视图切换**：修复在"我的歌单"页面退出登录后，页面停留在原处无法返回的问题。退出登录时会自动检测当前视图，如果在需要登录的视图则自动切换到音乐页面

#### 2026-07-08

### 功能优化
- **登录有效期**：登录状态有效期调整为7天，超过7天未重新登录将自动失效
- **记住密码**：登录界面新增"记住密码"选项，勾选后下次打开登录框自动填充用户名和密码
- **系统设置滚动优化**：系统设置二级菜单点击滚动时向上偏移10px，避免内容被顶部遮挡
- **封面悬停预览**：鼠标悬停在封面图片上时，在旁边弹出放大预览图（300x300），支持播放栏、歌单卡片、歌单详情等位置的封面
- **客户端播放状态持久化**：客户端播放模式下，刷新网页后自动恢复播放位置和播放状态，无需重新选择歌曲
- **歌曲来源标签**：所有歌曲列表（播放列表、歌单详情、文件管理、播放历史、搜索结果、缓存列表等）新增来源出处标签，本地音乐显示绿色"本地"标签，在线音乐显示对应平台主题色标签（网易云红色、QQ音乐绿色、酷我橙色等），样式统一为胶囊形状+图标+文字
- **听歌统计界面优化**：听歌统计页面样式与其他界面统一，使用相同的section-header风格和白色内容面板，优化概览卡片布局，固定卡片高度，文字溢出自动省略
- **所有列表添加封面显示**：播放列表、文件管理、我的歌单详情、在线音乐搜索/排行榜、在线歌单详情、缓存列表、播放历史、听歌统计常听歌曲等所有歌曲列表均添加封面显示，无封面时显示渐变色音乐图标模拟封面，封面悬停预览功能同步支持所有新增封面
- **歌单封面优化**：用户歌单封面优先使用歌单内第一首在线音乐的封面，无需手动设置，歌单列表和歌单详情页自动显示

### 新增功能
- **喜欢/收藏歌曲**：登录后可收藏喜欢的歌曲，自动保存到"我喜欢的音乐"歌单，播放栏心形按钮一键收藏/取消，歌单自动置顶且不可删除
- **播放历史记录**：自动记录最近播放的200首歌曲，侧边栏新增"播放历史"入口，支持查看播放时间、直接播放、添加到播放列表、播放全部、清空历史等操作
- **播放速度调节**：客户端播放模式下支持0.5x~2.0x倍速播放，共6档速度可选，设置自动保存，服务端模式提示不支持
- **睡眠定时器**：支持10/20/30/60/90分钟定时停止播放，按钮实时显示倒计时，时间到自动停止播放并通知
- **快捷键支持**：支持常用播放控制快捷键：空格（播放/暂停）、←→（快退/快进5秒）、↑↓（音量加减）、N（下一首）、P（上一首）、M（静音）、L（收藏）、F（大播放界面）、ESC（关闭大播放界面）
- **深色/浅色主题切换**：支持浅色模式、深色模式、跟随系统三种主题，在系统设置-界面设置中切换，设置自动保存，主要界面元素已适配主题
- **歌曲间淡入淡出**：支持歌曲切换时音量平滑过渡，可开关和调节时长（1-10秒），在系统设置-播放效果中配置，仅客户端模式有效
- **听歌统计**：侧边栏新增"听歌统计"入口，支持今日/本周/本月/全部四个时间维度，展示播放次数、听歌时长、最爱歌曲、最爱歌手，以及常听歌曲Top10和常听歌手Top10
- **均衡器/音效调节**：支持8种预设音效（原声、流行、摇滚、古典、爵士、人声、重低音、高音增强）和10段自定义均衡器（32Hz-16kHz，每段±12dB），在系统设置-播放效果中打开，仅客户端模式有效

### 问题修复
- **登录状态持久化**：修复服务器重启后已登录用户自动掉线的问题。原因是登录令牌仅存储在内存中，服务重启后丢失。现在将令牌持久化到 `data/auth_tokens.json`，登录/登出时自动保存，启动时自动恢复
- **登录窗口关闭按钮样式**：修复登录窗口关闭按钮位置异常和×字符不居中的问题，统一所有模态框关闭按钮样式
- **上传音源模态框关闭按钮**：修复上传音源文件模态框关闭按钮缺少样式定义的问题，统一使用 `.modal-close` 类
- **播放历史为空**：修复客户端播放模式下播放历史记录为空的问题。原因是客户端模式直接用 audio 播放，未调用后端接口记录历史，现在客户端播放时也会调用后端接口同步状态和记录历史
- **清空播放历史报错**：修复点击"清空历史"按钮时报 `confirmModal is not defined` 错误，改为使用自定义确认模态框

### 新增功能
- **文件夹视图增强**：文件管理页面新增按艺术家、专辑、流派、年份分组浏览功能，顶部视图切换标签一键切换，支持网格卡片展示和点击查看分组内歌曲列表
- **重复文件检测**：支持三种检测方式（按歌名+歌手、按文件名、按文件大小），自动统计重复组数、重复文件数和可释放空间，支持逐组展开查看和删除重复文件
- **歌单导入/导出**：支持 JSON 和 M3U 两种格式的歌单导入导出，导入时自动匹配音乐库中的文件，支持拖拽上传和点击选择文件，导出时自动下载对应格式文件
- **专辑封面管理**：统一管理所有专辑封面，网格展示所有专辑及其封面，支持按专辑名/艺术家搜索，点击专辑可跳转到对应专辑的歌曲列表，自动识别嵌入封面和文件夹封面（cover.jpg/folder.jpg等）
- **歌曲丢失检测**：歌单详情页自动检测本地音乐文件是否存在，丢失的歌曲显示红色"丢失"徽章，整体降低透明度，禁用播放/添加/下载按钮，歌单标题显示丢失数量，提供一键清理丢失歌曲功能
- **丢失歌曲重新匹配**：支持为丢失的歌曲在音乐库中搜索匹配文件，根据歌名、歌手、专辑、时长综合评分排序，支持手动选择匹配和一键自动匹配（匹配度高于60分自动替换）

### 功能优化
- **歌单播放性能优化**：播放歌单时，本地音乐从逐首添加改为批量添加，大幅提升加载速度，自动跳过不存在的文件，最后提示实际添加数量和丢失数量
- **移动端文件管理按钮优化**：文件管理页面在移动端隐藏"重复检测"和"封面管理"按钮，与其他操作按钮一致收纳到更多菜单

#### 2026-07-06

### 功能优化
- **权限控制优化**：优化未登录状态下的按钮显示逻辑，改用 CSS body class 控制，简化实现。文件管理菜单按钮、批量操作栏不再整体隐藏，菜单内部选项单独控制。系统设置、任务管理页面对所有用户可见，仅删除等敏感操作需要登录。任务列表中单个任务的删除按钮需要登录才能看到

### 问题修复
- **批量下载重复触发**：修复批量下载完成后弹出多个下载包的问题。原因是轮询请求并发导致完成状态被多次检测到，改用 `setTimeout` 递归调用 + 完成状态标记，确保只触发一次下载

### 功能优化
- **批量下载进度显示**：批量下载到本地时，新增右下角浮动进度卡片，不影响其他操作，实时显示当前处理进度、已处理文件数和当前文件名，打包完成后自动下载。支持播放列表批量下载和文件管理批量下载两个入口
- **批量下载性能优化**：后端改为异步打包模式，使用 `setTimeout` 延迟启动确保响应及时返回，每处理一个文件后让出事件循环，前端轮询间隔缩短到 100ms，进度更新更及时

#### 2026-07-05

### 新增功能
- **在线音乐下载到服务端**：登录后下载在线音乐时，可选择"下载到本地"或"下载到服务端"。下载到服务端支持自由选择保存目录（支持新建文件夹），默认选中 `music/在线下载` 目录，下载任务加入后台任务队列管理，支持设置并发下载数。下载到本地无需登录即可使用
- **全入口覆盖**：在线音乐搜索结果、歌单详情、我的歌单、播放列表、播放栏（需缓存完成）所有下载入口均支持下载方式选择
- **任务管理增强**：任务列表统一管理转换、上传、下载三种任务类型，新增"进行中"状态筛选，支持查看和删除下载任务

### 问题修复
- **文件名非法字符**：修复歌手名或歌曲名包含 `/`、`\`、`:` 等文件系统非法字符时，下载到服务端报 `ENOENT` 目录不存在错误的问题
- **批量下载到服务端无反应**：修复播放列表批量下载时选择"下载到服务端"点击确认后无反应的问题。原因是 `closeDownloadChoiceModal()` 会清空批量下载状态，导致后续条件判断不通过
- **批量下载到服务端 400 错误**：修复播放列表中在线音乐缺少 `songId` 顶层字段，导致批量下载到服务端时所有歌曲被跳过、返回"没有可下载的在线歌曲"400 错误的问题。同时修复添加在线音乐到播放列表时未传递完整 `songInfo` 的问题
- **任务列表重复显示**：修复任务列表中下载/上传任务重复显示的问题。原因是 `/api/tasks` 接口将队列中、进行中、已完成三个列表合并，但 `completedDownloads`/`completedUploads` 已包含所有状态的任务，导致重复。同时修复"清除已完成任务"会误删队列中任务的问题

### 功能优化
- **批量下载进度显示**：批量下载到本地时，新增打包进度弹窗，实时显示当前处理进度、已处理文件数和当前文件名，打包完成后自动下载。支持播放列表批量下载和文件管理批量下载两个入口

#### 2026-07-04

### 功能优化
- **大播放界面全屏按钮**：大播放界面右上角新增全屏按钮，点击可切换浏览器全屏模式，全屏时图标自动切换为退出全屏，关闭大播放界面时自动退出全屏

## [1.2.4]

#### 2026-07-04

### 新增功能
- **修改昵称**：个人中心支持直接修改昵称，点击昵称旁的编辑图标即可原地编辑保存，无需跳转页面
- **自定义头像**：支持上传自定义头像，个人中心点击顶部头像即可更换，支持 JPG/PNG/GIF/WebP/BMP 等常见图片格式（最大 5MB），侧边栏头像同步更新
- **我的歌单**：登录后侧边栏显示"我的歌单"入口，支持创建多个歌单、重命名、删除歌单，播放列表、文件管理（文件夹、单首歌曲）、在线音乐（搜索结果、排行榜、歌单详情）的歌曲均可添加到歌单，电脑端直接显示"添加到歌单"按钮，移动端通过更多菜单操作，支持播放歌单内全部歌曲

### 功能优化
- **我的歌单播放全部**：优化"播放全部"功能，支持本地歌曲和在线歌曲混合的歌单，点击后将歌单内所有歌曲（包括本地和在线音乐）全部加入播放列表并从第一首开始播放
- **我的歌单曲播放**：点击歌单内单首歌曲播放时，不再清空当前播放列表，而是将歌曲追加到播放列表后立即播放，保留原有播放列表内容
- **我的歌单卡片播放按钮**：歌单列表每个卡片右下角新增播放按钮，电脑端鼠标悬停显示，移动端默认显示，点击直接播放整个歌单
- **播放栏播放列表按钮**：底部播放控制栏新增播放列表按钮，点击后在右下角弹出浮动播放列表面板，显示当前播放列表，点击歌曲可直接切换播放，面板与主播放列表实时同步
- **浮动播放列表面板**：支持点击外部区域关闭，移动端隐藏播放栏按钮，大播放界面中自动切换深色风格与界面统一
- **播放模式图标**：顺序播放模式图标从列表图标改为流式播放图标，避免与播放列表按钮图标混淆
- **播放栏音乐封面**：播放栏左侧新增当前播放歌曲的封面图，无封面时显示紫色渐变音符图标，支持在线音乐和本地音乐封面
- **大播放界面**：点击播放栏封面打开全屏大播放界面，左侧大尺寸圆形封面（播放时旋转动画），右侧滚动歌词显示，支持进度条拖动、播放控制，移动端自动切换上下布局，背景根据封面生成模糊效果
- **播放状态图标**：播放列表中当前播放歌曲的状态图标从播放按钮改为喇叭图标，更直观表示正在播放
- **在线歌词匹配**：支持自动在线匹配歌词，本地音乐和在线音乐均可自动搜索并加载歌词，支持网易云音乐、酷我音乐、酷狗音乐三大平台，歌词实时同步滚动显示，当前行高亮放大

### Bug修复
- **登录界面设置密码按钮隐藏**：已设置密码（非首次使用）时，登录界面底部的"首次使用请先设置密码"提示和"设置密码"按钮会自动隐藏，避免误导
- **首次设置密码后用户卡片不更新**：修复首次设置密码后，侧边栏用户卡片仍显示"访客模式"的问题。设置密码成功后会立即更新用户卡片状态为"未登录"
- **设置密码后登录表单状态错误**：修复首次设置密码成功后跳转到登录表单时，底部设置密码按钮未正确隐藏的问题
- **静音切换失败**：修复客户端播放模式下点击静音按钮报错 `ReferenceError: updateMuteButton is not defined` 的问题，改用已有的 `updateVolumeIcon()` 函数更新音量图标状态
- **大播放界面播放列表按钮无反应**：修复大播放界面中点击播放列表按钮没有反应的问题，提升浮动面板 z-index 层级使其显示在大播放界面之上
- **浮动播放列表点击内部关闭**：修复点击浮动播放列表内部（如切换歌曲）会意外关闭面板的问题，列表项点击事件添加 `stopPropagation()` 阻止事件冒泡
- **个人中心重复头像**：修复个人中心模态框中出现两个头像的问题，统一为顶部一个头像，支持点击上传更换
- **大播放界面进度条排版错乱**：修复进度条外层容器和内层进度条共用同一类名导致的样式冲突，进度条与时间显示恢复正常布局
- **大播放界面封面显示不全**：优化大播放界面封面和歌词区域的响应式布局，封面尺寸根据视口高度自适应，避免小屏幕下显示不全，歌词区域高度自适应并保持居中滚动效果

#### 2026-07-03

### 新增功能
- **账号密码登录**：支持设置管理员账号密码，侧边栏新增用户头像卡片，未设置密码时为访客模式可自由使用播放功能，设置密码后管理操作（上传、删除、修改配置等）需要登录才能执行。支持修改密码、退出登录，密码使用 PBKDF2 哈希存储，Token 存储在 localStorage

### 功能优化
- **服务端播放下载按钮即时显示**：在线音乐在服务器声卡播放模式下，下载按钮无需等待缓存完毕即可显示，客户端浏览器播放模式保持原有逻辑
- **移动端播放切换按钮优化**：移动端播放栏的客户端/服务端切换按钮仅显示图标，隐藏文字以节省空间

### Bug修复
- **未登录先弹删除确认框**：修复未登录状态下点击删除/上传等管理操作时，先弹出操作确认框再弹出登录框的问题。新增 `requireLogin()` 通用函数，所有管理操作（删除、上传、重命名、移动、转换、刮削、整理、系统设置、应用音源等 30+ 个功能）在打开确认框或执行前先检查登录状态，未登录则先弹出登录框，登录成功后继续原操作
- **移动端用户卡片适配**：修复移动端侧边栏用户卡片头像、用户名显示异常的问题。移动端侧边栏宽度仅 70px，用户卡片改为只显示头像，隐藏用户名和箭头，缩小头像尺寸适配窄侧边栏
- **遗漏fetchWithAuth清理**：修复刮削元数据、上传音乐、上传音源等 3 处仍在使用已移除的 `fetchWithAuth` 函数的问题，全部改为使用全局 `fetch`
- **系统更新认证缺失**：修复手动系统更新和自动更新的认证问题。手动更新使用 `XMLHttpRequest` 不走全局 fetch 拦截，已添加 `Authorization` header 和登录检查；自动更新接口 `/api/auto-update` 已添加 `requireAuth` 权限验证
- **退出登录确认框不关闭**：修复点击退出登录确认后，确认框不关闭的问题，`doLogout` 函数已添加 `closeLogoutModal()` 调用
- **系统设置登录检查**：为系统设置页面中的管理操作添加登录检查，包括：播放输出切换、声卡设备选择、声卡测试、缓存大小设置、音乐目录设置、扫描文件夹、Worker线程配置、在线音乐设置
- **播放输出切换状态回退**：修复未登录状态下点击切换播放输出，取消登录后单选按钮选中状态未恢复的问题。现在取消登录或切换失败时，都会恢复到原来的选中状态
- **缓存大小设置状态回退**：修复未登录状态下点击缓存大小预设值，取消登录后单选按钮选中状态未恢复的问题。现在取消登录或设置失败时，都会恢复到原来的选中状态
- **在线音源切换登录检查与状态回退**：修复在线音源切换缺少登录检查、取消登录后下拉框选中状态未恢复的问题。已添加 `requireLogin()` 登录检查，取消登录或保存失败时恢复到原来的选中状态
- **在线音乐设置状态回退**：修复在线音乐分页大小设置取消登录后下拉框选中状态未恢复的问题。现在取消登录或保存失败时，都会恢复到原来的选中状态和分页配置
- **音源列表刷新无反馈**：修复音源文件管理页面点击"刷新列表"没有任何视觉反馈的问题。现在刷新时按钮图标会旋转动画，刷新失败会显示提示
- **音源列表刷新未重置勾选**：修复音源列表刷新后未应用的勾选状态不重置的问题。现在刷新列表会将勾选状态重置为后端实际激活的音源，未应用的临时勾选会被清除
- **登录界面设置密码按钮隐藏**：已设置密码（非首次使用）时，登录界面底部的"首次使用请先设置密码"提示和"设置密码"按钮会自动隐藏，避免误导
- **首次设置密码后用户卡片不更新**：修复首次设置密码后，侧边栏用户卡片仍显示"访客模式"的问题。设置密码成功后会立即更新用户卡片状态为"未登录"
- **认证机制重复导致登录死循环**：修复前端同时存在 `fetchWithAuth` 和全局 `window.fetch` 拦截两套认证机制的问题，移除冗余的 `fetchWithAuth`，统一使用全局拦截，避免重复处理 401 导致登录弹窗死循环
- **登录密码错误重复弹登录框**：修复登录接口 `/api/user/login` 返回 401（密码错误）时被全局拦截再次弹出登录框的问题，添加 `NO_AUTH_URLS` 白名单，登录、设置密码等认证相关接口不触发登录弹窗
- **取消登录后错误提示**：修复 `closeLoginModal` 函数引用不存在的 `pendingAuthRequest` 变量的问题，改为正确处理 `pendingAuthRequests` 数组中的所有 pending 请求，用户取消登录时统一 reject，避免状态不一致
- **管理接口权限遗漏**：为 16 个遗漏的管理接口添加 `requireAuth` 权限验证，包括：格式转换 `/api/convert-to-mp3`、任务删除 `/api/tasks/:taskId`、任务清空 `/api/tasks`、取消转换 `/api/cancel-convert`、解散文件夹 `/api/dissolve-folder`、系统更新 `/api/system-update`、自定义音源管理（验证/上传/导入/切换/排序/删除）、删除音源文件 `/api/source-files/:filename`、应用音源 `/api/apply-sources`、上传音源 `/api/upload-source`、在线音乐下载 `/api/download-online`、音源切换 `/api/online-source`、声卡测试 `/api/test-audio`

#### 2026-07-02

### 新增功能
- **缓存大小限制**：支持设置在线音乐缓存的最大容量（不限制 / 500MB / 1GB / 2GB / 自定义），超出限制时自动删除最旧的缓存文件
- **播放缓冲进度显示**：客户端播放模式下，进度条显示半透明紫色缓冲层，直观展示已下载/缓存的进度
- **播放栏下载按钮**：播放控制栏新增下载按钮，播放任意歌曲时都可快速下载，在线音乐根据缓存状态显示不同提示

### 功能优化
- **删除单个缓存使用模态框**：将删除单个缓存的浏览器原生确认框改为自定义模态框，与项目整体风格保持一致
- **系统设置列表高度约束**：声卡设备列表、音源文件列表增加最大高度限制（桌面端 300px / 移动端 250px），内容过多时列表内部滚动

### Bug修复
- **播放栏下载按钮不显示**：修复在线音乐播放时下载按钮不显示的问题，改为播放时就显示按钮，通过 tooltip 提示缓存状态
- **下载按钮点击无反应**：修复 `songId` 读取路径错误（从 `track.songId` 改为兼容 `track.songInfo.songId` 及从路径解析）和缓存列表字段名错误（`result.caches` → `result.cacheList`）
- **缓存下载提示文件不存在**：修复服务端缓存下载接口中 `getCacheFilePath` 参数传递错误，导致无法正确找到缓存文件的问题
- **客户端模式切歌变服务端播放**：修复客户端播放模式下，点击上一首/下一首或播放结束自动切歌后，服务端也会同时播放音乐的问题。前后端双重修复：前端 `playNext()`/`playPrevious()` 客户端模式下直接调用 `playTrack()` 走客户端播放；服务端 `/api/next`、`/api/previous`、`/api/play/:index`、`POST /api/play`、`POST /api/folder-play` 及 `client-playback-state` ended 分支在客户端模式下只更新状态不同时启动服务端播放；`playMusic` 和 `playOnlineMusic` 内部的自动切歌逻辑在客户端模式下也不再执行；前端 `playFolder()` 和 `playTrackFromFolder()` 客户端模式下改为走客户端播放
- **在线音乐播放链接失效重试**：修复 `/api/stream/:index` 接口中，在线音乐缓存的 `playUrl` 过期失效时请求失败返回 500 的问题。现在如果使用缓存链接请求失败，会自动重新获取播放链接并重试一次，切换播放模式或长时间暂停后继续播放更稳定
- **歌单播放全部功能失效**：修复在线音乐歌单"播放全部"功能因缺少 `/api/clear-playlist` 接口导致 404 错误的问题。新增服务端清空播放列表接口，同时前端 `playAllSonglistSongs()` 增加客户端模式支持
- **停止播放后缓冲进度未清除**：修复停止播放后，播放进度条上的缓冲进度（半透明紫色层）仍然显示的问题。`stopClient()` 和 `stop()` 函数停止播放时都会调用 `clearBufferProgress()` 清除缓冲进度显示

## [1.2.3]

#### 2026-07-02

### 新增功能
- **在线音乐自动缓存**：播放过的在线音乐会自动保存到本地缓存（`data/cache/`），下次播放直接读取本地文件，加载更快
- **缓存可视化管理**：播放管理页面新增"在线音乐缓存"卡片，显示缓存数量、总大小、缓存列表
- **缓存管理API**：新增 `/api/cache`（获取列表）、`/api/cache/:cacheKey`（删除单个）、`/api/cache`（清空全部）接口

### 功能优化
- **播放设置整合到系统设置**：将原"播放管理"页面的所有功能（播放输出方式、声卡设备管理、在线音乐缓存）整合到"系统设置"页面，简化导航结构，减少侧边栏菜单项
- **系统设置二级菜单**：系统设置页面新增左侧二级导航菜单，支持快速跳转到播放输出、声卡设备、音乐缓存、音源文件、搜索设置等模块，滚动时自动高亮当前模块
- **列表卡片样式统一**：声卡设备列表、音源文件列表采用与播放输出方式一致的卡片式设计，2px边框、圆角、悬停和选中状态视觉统一
- **清空缓存确认模态框**：将清空缓存的浏览器原生确认框改为自定义模态框，与项目整体风格保持一致
- **系统设置移动端优化**：针对移动端优化系统设置页面所有列表的样式，缩小间距和字号，保持舒适点击区域的同时显示更多内容
- **缓存智能处理**：播放完成保存完整缓存，播放出错自动清理不完整缓存，用户提前断开保留大于100KB的部分缓存
- **缓存支持进度拖动**：缓存文件支持 Range 请求，可自由拖动进度条
- **缓存元数据记录**：记录歌曲标题、艺术家、专辑、音源平台、缓存时间等信息

### Bug修复
- **客户端播放切换误报错误**：修复从客户端浏览器切换到服务器声卡时，因设置 `clientAudio.src = ''` 触发 error 事件导致的误报错误，添加 `isStoppingClient` 标志位区分主动停止和真正的播放错误

#### 2026-07-01

### 新增功能
- **客户端播放模式**：新增播放输出方式选择，支持"服务器声卡"和"客户端浏览器"两种模式
- **音频流式传输**：后端新增 `/api/stream/:index` 接口，支持本地和在线音乐的流式播放，支持 Range 请求（seek）
- **播放状态同步**：客户端播放时自动同步播放状态（播放、暂停、结束）到服务器，保持多端状态一致
- **播放管理页面**：将"声卡管理"更名为"播放管理"，新增播放输出方式选择卡片

### 功能优化
- **播放输出切换**：切换播放输出方式时自动停止旧模式播放并以新模式重新播放
- **客户端播放控制**：支持客户端模式下的播放、暂停、停止、音量调节、静音、进度拖拽跳转
- **多模式兼容**：播放模式（顺序、随机、单曲、循环）在客户端模式下正常工作
- **进度显示优化**：客户端模式下使用 HTML5 Audio 的 timeupdate 事件实时更新进度，减少服务端请求
- **播放栏快捷切换**：播放栏新增播放输出方式快捷切换按钮，桌面端显示在音量控制右侧，移动端显示在音量控制下方
- **快捷按钮状态同步**：通过快捷按钮切换播放输出时，同步更新播放管理页面的单选按钮状态

### Bug修复
- **客户端播放在线音乐失败**：修复使用 Web Streams API 时 `response.body.pipe` 方法不存在的问题，改用 `reader.read()` 方式正确代理音频流
- **在线音乐客户端播放**：修复 `/api/play-online` 端点在客户端模式下不返回播放列表的问题，确保在线音乐能正确通过客户端播放
- **在线搜索/歌单播放**：修复 `playOnlineSong` 和 `playSonglistSong` 函数在客户端模式下未调用客户端播放的问题

#### 2026-06-07

### 功能优化
- **在线音乐搜索框位置调整**：搜索框从顶部栏移到搜索结果区域内，布局更合理
- **排行榜头部布局优化**：排行榜类型选择、歌曲数量、分页按钮、刷新按钮在一行显示
- **歌单头部布局优化**：最热下拉框、全部分类、搜索按钮、分页按钮、刷新按钮在一行显示
- **搜索结果头部布局优化**：搜索框居中显示，标题和分页按钮左右对齐
- **在线音乐移动端菜单**：移动端时显示菜单按钮（⋮），点击弹出播放、下载、添加到播放列表操作菜单
- **歌单详情移动端按钮**：返回歌单和播放全部按钮在移动端只显示图标，节省空间
- **搜索结果区域独立样式**：添加 `.online-search-results` 独立样式类
- **空状态样式美化**：为搜索结果、播放列表、歌单的空状态添加美观样式和动画效果
- **加载状态样式优化**：为排行榜、歌单、搜索等加载状态添加美观样式和动画效果

### Bug修复
- **搜索结果和分页排列问题**：修复搜索结果区域标题和分页按钮排列异常问题
- **移动端布局问题**：修复搜索结果区域在移动端布局上下颠倒的问题

## [1.2.2]

#### 2026-06-06

### 新增功能
- **在线歌单功能**：新增在线音乐歌单功能，支持网易云、QQ音乐、咪咕音乐、酷我音乐、酷狗音乐五个平台
- **歌单标签浏览**：支持按标签分类浏览歌单
- **歌单搜索**：支持搜索歌单
- **歌单详情**：查看歌单详情和歌曲列表
- **歌单播放**：支持播放歌单中的单曲或全部播放

### 功能优化
- **文件夹右键菜单**：文件管理页面支持文件夹右键菜单，与文件右键菜单功能一致
- **菜单互斥显示**：所有右键菜单和弹出菜单互斥显示，防止同时出现多个菜单
- **移动端菜单优化**：移动端文件夹菜单按钮改为动态弹出方式，与音乐文件菜单一致
- **大屏幕菜单按钮隐藏**：大屏幕时隐藏文件夹卡片的菜单按钮，使用右键菜单操作
- **操作按钮样式统一**：文件夹、音乐文件、在线音乐、音源文件、任务管理的操作按钮统一为透明方形样式
- **声卡管理列表样式优化**：参考音源文件管理卡片设计，使用独立样式类名，保持外观一致
- **声卡设备状态显示**：添加"已选中/未选中"状态标签，切换设备时状态正确更新
- **更新系统本地访问检测** ：当访问域名是 1.0.1.0.1 或 localhost 时，自动禁用检查更新和手动更新功能，显示提示通知
- **文件管理删除确认** ：删除音源文件时使用模态框确认，替代浏览器原生 confirm 对话框
- **任务管理清除确认** ：清除任务时使用模态框确认，替代浏览器原生 confirm 对话框
- **播放列表批量操作按钮样式统一** ：批量操作按钮与播放列表操作按钮样式一致（透明背景、蓝色图标）
- **文件管理批量操作按钮样式统一** ：批量操作按钮与播放列表操作按钮样式一致（透明背景、蓝色图标）
- **浏览器右键菜单拦截** ：在页面上拦截浏览器默认右键菜单，确保自定义右键菜单正常工作
- **歌单按钮样式统一**：歌单详情的播放和添加按钮样式与播放列表保持一致（透明背景、蓝色图标）
- **歌单详情独立样式**：歌单详情结果区域使用独立样式，灰色背景区分内容区域

### Bug修复
- **排行榜 pageSize 设置不生效**：修复在线音乐和文件夹视图共享分页设置导致的问题
- **排行榜分页页码不更新**：修复点击上一页时列表变化但页码不更新的问题
- **自定义音源播放链接获取**：修复 songInfo 字段标准化问题，确保各平台特有字段正确传递
- **自定义音源搜索失败**：改进 lx.request 函数，修复搜索请求失败问题
- **歌单播放API调用错误**：修复播放API端点调用错误，从 POST /api/play 改为 GET /api/play/:index
- **refreshPlaylist 未定义**：修复函数名错误，改为 loadPlaylistData
- **网易云歌单详情API**：添加 linuxapi 加密支持，修复歌单详情获取失败问题
- **QQ音乐歌单详情API**：添加 Origin 和 Referer 请求头，修复获取失败问题
- **httpFetch 返回值问题**：修复返回 { promise } 对象而非直接返回 promise 的问题

### 新增 API
- `GET /api/online-songlist-tags` - 获取歌单标签列表
- `POST /api/online-songlist-list` - 获取歌单列表
- `POST /api/online-songlist-detail` - 获取歌单详情
- `POST /api/online-songlist-search` - 搜索歌单
- `GET /api/online-songlist-sources` - 获取支持歌单的平台列表

#### 2026-06-04

### 功能优化
- **排行榜分页功能**：为在线音乐排行榜添加分页浏览功能，支持上下页切换
- **排行榜分页位置**：分页控件移到排行榜右上角，与搜索结果保持一致
- **在线音乐分页独立**：在线音乐的分页设置与文件夹视图分离，避免切换视图时重置
- **自定义音源 songInfo 标准化**：参考 lx-music-sync-server 实现 songInfo 字段标准化，将 meta 字段提升到顶层
- **lx.utils.crypto 模块完善**：添加 sha1、sha256、aesEncrypt2、aesDecrypt2、rsaDecrypt、hmac 等加密工具函数
- **lx.utils.zlib 模块完善**：添加 inflateRaw、deflateRaw 压缩工具函数
- **lx.request 函数改进**：添加 HTTPS 证书忽略、默认 User-Agent、改进超时处理

### Bug修复
- **排行榜 pageSize 设置不生效**：修复在线音乐和文件夹视图共享分页设置导致的问题
- **排行榜分页页码不更新**：修复点击上一页时列表变化但页码不更新的问题
- **自定义音源播放链接获取**：修复 songInfo 字段标准化问题，确保各平台特有字段正确传递
- **自定义音源搜索失败**：改进 lx.request 函数，修复搜索请求失败问题

## [1.2.1]

#### 2026-06-03

### 新增功能
- **在线音乐搜索**：支持从酷我、酷狗、QQ音乐、网易云音乐、咪咕音乐等多个平台搜索音乐
- **在线音乐播放**：支持直接播放搜索到的在线音乐，无需下载
- **在线音乐视图**：新增独立的在线音乐界面，默认显示排行榜
- **音乐排行榜**：在线音乐页面默认显示排行榜，支持热门榜、新歌榜、飙升榜、原创榜
- **音源文件管理**：支持上传、删除、刷新自定义音源文件（.js 格式），支持勾选启用/禁用
- **多音源选择**：音源文件列表支持复选框多选，点击"应用选中"按钮启用选中的音源
- **文件夹加入播放列表**：支持将文件夹（递归扫描）中的所有音乐文件添加到播放列表
- **文件夹下载**：支持整个文件夹打包下载
- **文件夹转MP3**：支持批量转换文件夹中的音频文件为 MP3 格式
- **批量添加到播放列表**：支持批量选择文件并添加到播放列表
- **批量删除文件**：支持批量删除音乐文件，并自动更新播放列表
- **批量下载**：支持批量下载选中的音乐文件
- **批量移动文件**：支持批量移动文件到指定文件夹
- **批量转换MP3**：支持批量转换音频文件为 MP3 格式
- **音乐文件整理**：支持自动整理音乐文件
- **元数据刮削**：支持从网络获取音乐文件的元数据（封面、艺术家、专辑等）
- **播放列表来源标识**：播放列表中显示每首歌曲的来源（本地音乐/在线音乐平台）

### 功能优化
- **LX Music 音源兼容**：使用 VM 沙箱加载 LX Music 格式的音源文件，支持 `lx.send('inited', { sources })` 注册方式
- **完整歌曲信息传递**：在线播放时传递完整的歌曲信息（hash、songmid、albumId 等）给自定义音源，确保播放链接正确获取
- **系统设置视图优化**：进入系统设置时自动加载音源选中状态，无需手动刷新
- **文件夹卡片操作按钮**：桌面端文件夹卡片新增加入播放列表、移动、下载、转MP3 等快捷按钮
- **任务系统重构**：重构转换任务系统，支持多线程并行转换和实时进度推送
- **在线音乐播放控制**：修复在线音乐的暂停、停止、进度控制功能
- **在线音乐列表合并**：多个平台的搜索结果合并显示为统一列表，带平台标识
- **播放列表持久化**：在线音乐添加到播放列表后自动保存到缓存，重启服务器后不丢失
- **在线音乐时长显示**：修复排行榜和搜索列表中时长显示格式问题，正确显示为 mm:ss 格式
- **在线音乐进度同步**：修复在线音乐播放时进度条不更新的问题
- **播放切换优化**：修复播放列表中本地音乐和在线音乐切换异常问题，切换前先停止当前播放
- **自动播放下一首**：支持从在线音乐自动切换到本地音乐，或反之
- **在线播放重试机制**：添加播放失败自动重试功能，最多重试2次
- **网络连接优化**：添加 ffmpeg 网络重连参数，提高在线播放稳定性
- **音源请求头过滤**：自动过滤值为 undefined 或 null 的请求头，避免音源加载失败

### Bug修复
- **在线音乐播放失败降级**：当当前平台无法获取播放链接时，自动尝试从其他平台搜索播放
- **在线音乐添加到播放列表后无法播放**：修复播放列表中在线音乐的播放逻辑
- **本地音乐播放问题**：修复在线音乐修改导致本地音乐无法播放的问题
- **播放列表切换异常**：修复播放列表中本地音乐和在线音乐切换时各种异常问题
- **在线音乐时长显示错误**：修复排行榜和搜索列表中时长显示为原始数字而非格式化时间的问题
- **自定义音源加载失败**：修复音源脚本中请求头值为 undefined 导致的加载失败问题
- **播放列表保存问题**：修复播放在线音乐时没有保存到缓存文件的问题
- **音源脚本 vm 沙箱修复**：修复音源脚本中 `globalThis.lx` 未定义的问题，设置 `global`、`window`、`globalThis` 都指向 sandbox 自身
- **在线音乐进度条不更新**：修复从在线音乐搜索结果播放时进度条不动的问题，添加 `startProgressUpdate()` 调用

### 新增 API
- `POST /api/online-search` - 搜索在线音乐
- `POST /api/online-play-url` - 获取在线音乐播放链接
- `POST /api/play-online` - 播放在线音乐
- `GET /api/online-sources` - 获取支持的在线音乐源列表
- `POST /api/add-folder-to-playlist` - 添加文件夹到播放列表
- `GET /api/download-folder` - 下载整个文件夹
- `POST /api/batch-convert-folder-mp3` - 批量转换文件夹为 MP3
- `POST /api/batch-add-to-playlist` - 批量添加文件到播放列表
- `POST /api/batch-delete-files` - 批量删除文件
- `POST /api/batch-download` - 批量下载文件
- `POST /api/batch-move-files` - 批量移动文件
- `POST /api/scrape-metadata` - 刮削音乐元数据
- `POST /api/scrape-folder` - 批量刮削文件夹元数据
- `POST /api/organize` - 整理音乐文件
- `GET /api/tasks` - 获取任务列表
- `POST /api/cancel-convert` - 取消转换任务
- `POST /api/add-online-to-playlist` - 添加在线音乐到播放列表（不立即播放）

## [1.2.0]

#### 2026-06-02

### 新增功能
- **更新进度显示**：点击"立即更新"后显示实时下载、解压、安装进度条，使用 SSE 实时推送
- **移动文件时新建文件夹**：移动文件对话框支持直接输入新文件夹名称，点击确认移动时自动创建并移动文件
- **移动文件列表显示**：移动文件对话框显示当前待移动的文件列表和数量

### 功能优化
- **文件夹操作性能优化**：新建文件夹、删除文件、删除文件夹、重命名文件和文件夹时，不再清空缓存重新扫描整个目录，改为精细更新缓存数据
- **新建文件夹优化**：新建文件夹后只刷新当前文件夹视图，不重新加载整个缓存
- **移动对话框状态管理**：关闭移动文件对话框时自动清空文件列表和输入框，避免显示上次操作的内容
- **切换文件夹清空选中**：切换文件夹时自动清空之前勾选的文件，避免批量移动时误操作
- **文件夹卡片移动端菜单**：文件管理的文件夹卡片操作按钮在移动端改为菜单模式，点击"更多"按钮展开操作选项
- **批量操作移动端菜单**：文件管理的批量操作按钮在移动端改为菜单模式，点击"更多"按钮展开操作选项
- **移动端分页优化**：文件管理分页在移动端改为简洁模式，只显示导航按钮（首页、上一页、下一页、末页）和页码信息，避免按钮过多超出屏幕
- **桌面端快速跳转**：文件管理分页在桌面端添加页码输入框和跳转按钮，支持快速跳转到指定页码

### 新增 API
- `GET /api/update-progress` - SSE 端点，实时推送更新进度

## [1.1.0]

#### 2026-06-01

### 新增功能
- **文件夹删除**：文件管理支持删除文件夹，带确认对话框，自动清理播放列表中的相关文件
- **文件夹重命名**：文件管理支持重命名文件夹，自动更新播放列表中文件路径
- **音乐文件重命名**：文件管理支持重命名音乐文件，保留原扩展名，自动更新播放列表

### 功能优化
- **文件管理后端分页**：文件管理列表分页改为后端获取，提升大数据量下的加载性能
- **分页参数支持**：后端 `/api/folders` 接口支持 `page`、`pageSize`、`search` 参数
- **分页加载优化**：只解析当前页文件的元数据，避免全量解析导致的性能问题
- **搜索性能优化**：搜索时基于文件名快速过滤，只解析当前页结果文件的元数据
- **移动端菜单完善**：将重命名操作添加到移动端"更多"菜单中，保持界面简洁
- **按钮样式修复**：修复文件夹操作按钮与头部工具栏按钮样式冲突问题
- **图标区分**：重命名按钮使用 `fa-font` 图标，与编辑属性的 `fa-edit` 图标区分

### Bug修复
- **搜索功能修复**：修复搜索时路径匹配问题导致无法找到匹配文件的问题
- **分页按钮显示**：修复分页控件在文件数量超过页大小时不显示的问题

### 新增 API
- `DELETE /api/delete-folder` - 删除文件夹及其内容
- `POST /api/rename-folder` - 重命名文件夹
- `POST /api/rename-file` - 重命名音乐文件

### 安全特性
- **路径安全检查**：所有文件/文件夹操作都验证路径在 music 目录下
- **非法字符检查**：重命名时检查 `\ / : * ? " < > |` 等非法字符
- **同名冲突检测**：重命名时检查目标名称是否已存在

### 代码优化
- **统一数据加载**：文件操作（删除、移动等）后统一调用 `loadFolderData` 重新加载数据
- **移除前端分页逻辑**：删除前端手动分页和手动更新文件计数的代码
- **简化移动文件逻辑**：移动文件后直接重新加载当前视图，不再手动维护文件列表

## [1.0.9]

#### 2026-05-21

### 功能优化
- **release.ps1 打包重构**：将 PowerShell 打包逻辑替换为调用 package.js，统一打包流程
- **package.js 命令行支持**：支持通过命令行参数指定输出文件名

### Bug修复
- **server.js UTF-8 BOM 修复**：修复更新包验证时 version.json 的 BOM 问题
- **package.js 排除文件优化**：添加跳过 .git、.env、PROJECT_MEMORY.md、release.ps1 文件/目录

## [1.0.8]

#### 2026-05-21

### Bug修复
- **package.js UTF-8 BOM 修复**：修复读取 version.json 时因 BOM 头导致 JSON 解析失败的问题，现在能正确读取版本号

## [1.0.7]

#### 2026-05-21

### Bug修复
- **日常更新**：修复一些小问题，优化性能

## [1.0.6]

#### 2026-05-21

### Bug修复
- **version.json 路径匹配修复**：改进 GitHub ZIP 包中 version.json 的路径匹配逻辑

## [1.0.5]

#### 2026-05-21

### Bug修复
- **package.js 版本读取修复**：从 version.json 读取版本信息，修复硬编码 1.0.0 的问题

## [1.0.4]

#### 2026-05-21

### Bug修复
- **versionInfo 变量作用域修复**：修复更新包验证时 `versionInfo is not defined` 错误

## [1.0.3]

#### 2026-05-21

### 功能优化
- **更新确认模态框**：点击"立即更新"按钮时使用模态框替代浏览器 confirm 对话框

## [1.0.2]

#### 2026-05-21

### 新增功能
- **自动发布脚本优化**：改进发布流程，支持自动更新 CHANGELOG.md
- **中文编码修复**：修复发布脚本中中文乱码问题，使用 Unicode 码点构建中文字符串

### Bug修复
- **在线更新修复**：修复 `versionInfo is not defined` 错误
- **CHANGELOG 乱码修复**：修复自动更新 CHANGELOG.md 时的中文编码问题

### 功能优化
- **发布流程自动化**："发布"命令现在自动更新日志、递增版本号、创建 tag 和 Release

## [1.0.1]

#### 2026-05-21

### 新增功能
- **检查更新功能**：在"关于"视图添加检查更新按钮，自动从 GitHub 获取最新版本信息
- **版本动态显示**：页面自动加载并显示当前版本号（从 version.json 读取）
- **版本比较**：自动对比当前版本与 GitHub 最新版本，提示是否有新版本可用
- **更新下载链接**：发现新版本时提供 GitHub Release 页面链接

### 文档完善
- **依赖库说明**：在 README.md 中添加完整的依赖库说明表格，包含9个 npm 依赖的版本和用途
- **Dockerfile 注释**：完善 Dockerfile 注释，详细说明系统级依赖和 Node.js 依赖的用途

### 新增 API
- `GET /api/version` - 获取当前版本信息
- `GET /api/check-update` - 检查远程更新

## [1.0.0]

#### 2026-05-13

### 新增功能
- **进度条拖动功能**：支持点击和拖动进度条跳转到指定播放位置
- **音量记忆功能**：音量设置保存到配置文件，重启服务端后自动恢复

### Bug修复
- **播放/暂停图标切换**：修复点击暂停按钮后图标没有正确更新的问题
- **暂停时进度条位置**：修复暂停时进度条重置为0:00的问题，现在会停在当前位置

### 新增功能
- **ffmpeg 自动安装脚本**：创建 `install-ffmpeg.ps1`，支持一键下载安装 ffmpeg 并配置环境变量

### 功能优化
- **播放器栏样式**：`.player-bar` 背景改为半透明效果
- **刷新按钮位置**：移到播放列表右上角
- **移动端音量控制**：移到播放器栏右上角
- **播放列表定位**：切换歌曲时自动滚动定位到当前播放歌曲
- **声卡设备显示**：修复 Windows 中文设备名称乱码问题（GBK→UTF-8）
- **Windows 声卡切换**：改用 `ffplay` 播放器，支持声卡设备切换
- **多客户端音量同步**：添加音量状态同步，支持跨客户端音量同步

### Bug修复
- **响应式按钮**：修复 `font-size: 0` 导致的空白问题，添加 `line-height: 0` 和 `gap: 0`
- **多客户端进度条同步**：修复点击下一曲时进度条不同步的问题
- **更新保护**：确保 `.git` 和 `config.json` 在更新时被跳过

#### 2026-05-12

### 新增功能
- **多客户端状态同步**：新增状态同步机制，每3秒检查一次服务端状态，确保多个浏览器/电脑访问时播放状态一致
- **手动更新功能**：在"关于"视图添加手动更新按钮，支持ZIP格式更新包上传

### 功能优化
- **音量控制**：将默认音量从100%调整为70%，同步更新前后端配置
- **界面优化**：将"关于"从模态框改为独立视图，优化响应式设计
- **设备管理**：修复声卡设备列表选中状态问题，处理特殊字符ID

### Bug修复
- 修复页面加载时当前播放歌曲不显示的问题
- 修复更新模态框元素未找到的问题
- 修复上传进度条元素未找到的错误
- 删除重复的CSS样式定义和JavaScript函数

### 安全保护
- 更新时自动跳过`music`文件夹和`config.json`文件，保护用户音乐文件和配置不被覆盖

### 代码质量
- 清理`style.css`中的重复样式定义
- 删除`app.js`中的重复函数（`searchMusic`、`refreshLibrary`）
